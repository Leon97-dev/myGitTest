// sum
function getSum(a, b) {
  return a + b;
}

// subtract
function getSub(a, b) {
  return a - b;
}

// multiply
function getMul(a, b) {
  return a * b;
}

// divide
function getDiv(a, b) {
  return a / b;
}
여기까지 무료 버전입니다.
